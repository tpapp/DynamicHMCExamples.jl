var documenterSearchIndex = {"docs":
[{"category":"section","location":"example_linear_regression/#Linear-regression","page":"Linear regression","text":"We estimate simple linear regression model with a half-T prior. First, we load the packages we use.\n\nFirst, we import DynamicHMC and related libraries,\n\nusing TransformVariables, LogDensityProblems, LogDensityProblemsAD,\n    DynamicHMC, TransformedLogDensities\n\nthen some packages that help code the log posterior,\n\nusing Parameters, Statistics, Random, Distributions, LinearAlgebra\n\nthen diagnostic and benchmark tools,\n\nusing MCMCDiagnosticTools, DynamicHMC.Diagnostics, BenchmarkTools\n\nand use ForwardDiff for AD since the dimensions is small.\n\nimport ForwardDiff\n\nThen define a structure to hold the data: observables, covariates, and the degrees of freedom for the prior.\n\n\"\"\"\nLinear regression model ``y ∼ Xβ + ϵ``, where ``ϵ ∼ N(0, σ²)`` IID.\n\nWeakly informative prior for `β`, half-T for `σ`.\n\"\"\"\nstruct LinearRegressionProblem{TY <: AbstractVector, TX <: AbstractMatrix,\n                               Tν <: Real}\n    \"Observations.\"\n    y::TY\n    \"Covariates\"\n    X::TX\n    \"Degrees of freedom for prior.\"\n    ν::Tν\nend\n\nThen make the type callable with the parameters as a single argument.\n\nfunction (problem::LinearRegressionProblem)(θ)\n    @unpack y, X, ν = problem   # extract the data\n    @unpack β, σ = θ            # works on the named tuple too\n    ϵ_distribution = Normal(0, σ) # the error term\n    ℓ_error = mapreduce((y, x) -> logpdf(ϵ_distribution, y - dot(x, β)), +,\n                        y, eachrow(X))    # likelihood for error\n    ℓ_σ = logpdf(TDist(ν), σ)             # prior for σ\n    ℓ_β = loglikelihood(Normal(0, 10), β) # prior for β\n    ℓ_error + ℓ_σ + ℓ_β\nend\n\nMake up random data and test the function runs.\n\nN = 100\nX = hcat(ones(N), randn(N, 2));\nβ = [1.0, 2.0, -1.0]\nσ = 0.5\ny = X*β .+ randn(N) .* σ;\np = LinearRegressionProblem(y, X, 1.0);\np((β = β, σ = σ))\n\nIt is usually a good idea to benchmark and optimize your log posterior code at this stage. Above, we have carefully optimized allocations away using mapreduce.\n\n@btime p((β = $β, σ = $σ))\n\nFor this problem, we write a function to return the transformation (as it varies with the number of covariates).\n\nfunction problem_transformation(p::LinearRegressionProblem)\n    as((β = as(Array, size(p.X, 2)), σ = asℝ₊))\nend\n\nWrap the problem with a transformation, then use ForwardDiff for the gradient.\n\nt = problem_transformation(p)\nP = TransformedLogDensity(t, p)\n∇P = ADgradient(:ForwardDiff, P);\nnothing #hide\n\nFinally, we sample from the posterior. chain holds the chain (positions and diagnostic information), while the second returned value is the tuned sampler which would allow continuation of sampling.\n\nresults = map(_ -> mcmc_with_warmup(Random.default_rng(), ∇P, 1000), 1:5)\n\nWe use the transformation to obtain the posterior from the chain.\n\nposterior = transform.(t, eachcol(pool_posterior_matrices(results)));\nnothing #hide\n\nExtract the parameter posterior means: β,\n\nposterior_β = mean(first, posterior)\n\nthen σ:\n\nposterior_σ = mean(last, posterior)\n\nEffective sample sizes (of untransformed draws)\n\ness, R̂ = ess_rhat(stack_posterior_matrices(results))\n\nsummarize NUTS-specific statistics of all chains\n\nsummarize_tree_statistics(mapreduce(x -> x.tree_statistics, vcat, results))\n\n\n\nThis page was generated using Literate.jl.","title":"Linear regression"},{"category":"section","location":"example_logistic_regression/#Logistic-regression","page":"Logistic regression","text":"First, we import DynamicHMC and related libraries,\n\nusing TransformVariables, LogDensityProblems, LogDensityProblemsAD,\n    DynamicHMC, TransformedLogDensities\n\nthen some packages that help code the log posterior,\n\nusing Parameters, Statistics, Random, Distributions, LinearAlgebra, StatsFuns, LogExpFunctions\n\nthen diagnostic and benchmark tools,\n\nusing MCMCDiagnosticTools, BenchmarkTools\n\nand use ForwardDiff for AD since the dimensions is small.\n\nimport ForwardDiff\n\n\"\"\"\nLogistic regression.\n\nFor each draw, ``logit(Pr(yᵢ == 1)) ∼ Xᵢ β``. Uses a `β ∼ Normal(0, σ)` prior.\n\n`X` is supposed to include the `1`s for the intercept.\n\"\"\"\nstruct LogisticRegression{Ty, TX, Tσ}\n    y::Ty\n    X::TX\n    σ::Tσ\nend\n\nfunction (problem::LogisticRegression)(θ)\n    @unpack y, X, σ = problem\n    @unpack β = θ\n    ℓ_y = mapreduce((y, x) -> logpdf(Bernoulli(logistic(dot(x, β))), y), +, y, eachrow(X))\n    ℓ_β =  loglikelihood(Normal(0, σ), β)\n    ℓ_y + ℓ_β\nend\n\nMake up parameters, generate data using random draws.\n\nN = 1000\nβ = [1.0, 2.0]\nX = hcat(ones(N), randn(N))\ny = rand.(Bernoulli.(logistic.(X*β)));\nnothing #hide\n\nCreate a problem, apply a transformation, then use automatic differentiation.\n\np = LogisticRegression(y, X, 10.0)   # data and (vague) priors\nt = as((β = as(Array, length(β)), )) # identity transformation, just to get the dimension\nP = TransformedLogDensity(t, p)      # transformed\n∇P = ADgradient(:ForwardDiff, P)\n\nBenchmark\n\n@btime p((β = $β,))\n\nSample using NUTS, random starting point.\n\nresults = map(_ -> mcmc_with_warmup(Random.default_rng(), ∇P, 1000), 1:5)\n\nExtract the posterior. (Here the transformation was not really necessary).\n\nβ_posterior = first.(transform.(t, eachcol(pool_posterior_matrices(results))))\n\nCheck that we recover the parameters.\n\nmean(β_posterior)\n\nQuantiles\n\nqs = [0.05, 0.25, 0.5, 0.75, 0.95]\nquantile(first.(β_posterior), qs)\n\nquantile(last.(β_posterior), qs)\n\nCheck that mixing is good.\n\ness, R̂ = ess_rhat(stack_posterior_matrices(results))\n\n\n\nThis page was generated using Literate.jl.","title":"Logistic regression"},{"category":"section","location":"example_independent_bernoulli/#Estimate-Bernoulli-draws-probabilility","page":"Estimate Bernoulli draws probabilility","text":"We estimate a simple model of n independent Bernoulli draws, with probability α. First, we load the packages we use.\n\nFirst, we import DynamicHMC and related libraries,\n\nusing TransformVariables, LogDensityProblems, LogDensityProblemsAD, DynamicHMC,\n    TransformedLogDensities\n\nthen some packages that help code the log posterior,\n\nusing Parameters, Statistics, Random, Distributions, LinearAlgebra\n\nthen diagnostic tools,\n\nusing MCMCDiagnosticTools, DynamicHMC.Diagnostics\n\nand use ForwardDiff for AD since the dimensions is small.\n\nimport ForwardDiff\n\nThen define a structure to hold the data. For this model, the number of draws equal to 1 is a sufficient statistic.\n\n\"\"\"\nToy problem using a Bernoulli distribution.\n\nWe model `n` independent draws from a ``Bernoulli(α)`` distribution.\n\"\"\"\nstruct BernoulliProblem\n    \"Total number of draws in the data.\"\n    n::Int\n    \"Number of draws `==1` in the data\"\n    s::Int\nend\n\nThen make the type callable with the parameters as a single argument.  We use decomposition in the arguments, but it could be done inside the function, too.\n\nfunction (problem::BernoulliProblem)(θ)\n    @unpack α = θ               # extract the parameters\n    @unpack n, s = problem      # extract the data\n    # log likelihood: the constant log(combinations(n, s)) term\n    # has been dropped since it is irrelevant for posterior sampling.\n    s * log(α) + (n-s) * log(1-α)\nend\n\nWe should test this, also, this would be a good place to benchmark and optimize more complicated problems.\n\np = BernoulliProblem(20, 10)\np((α = 0.5, ))\n\nRecall that we need to\n\ntransform from ℝ to the valid parameter domain (0,1) for more efficient sampling, and\ncalculate the derivatives for this transformed mapping.\n\nThe helper packages TransformVariables and LogDensityProblems take care of this. We use a flat prior.\n\nt = as((α = as𝕀,))\nP = TransformedLogDensity(t, p)\n∇P = ADgradient(:ForwardDiff, P);\nnothing #hide\n\nFinally, we sample from the posterior. The returned value the posterior matrix, diagnostic information, and the tuned sampler which would allow continuation of sampling.\n\nresults = [mcmc_with_warmup(Random.default_rng(), ∇P, 1000) for _ in 1:5]\n\nTo get the posterior for α, we need to use the columns of the posterior_matrix and then transform\n\nposterior = transform.(t, eachcol(pool_posterior_matrices(results)));\nnothing #hide\n\nExtract the parameter.\n\nposterior_α = first.(posterior);\nnothing #hide\n\ncheck the mean\n\nmean(posterior_α)\n\ncheck the effective sample size\n\ness, R̂ = ess_rhat(stack_posterior_matrices(results))\n\nNUTS-specific statistics of the first chain\n\nsummarize_tree_statistics(results[1].tree_statistics)\n\n\n\nThis page was generated using Literate.jl.","title":"Estimate Bernoulli draws probabilility"},{"category":"section","location":"#Overview","page":"Overview","text":"This are automatically generated pages from DynamicHMCExamples.jl. Each page is for one example problem, you can study the source directly in the package repository.","title":"Overview"},{"category":"section","location":"#Implementation-notes","page":"Overview","text":"Since these examples are run in a CI environment and are reasonably fast, we don't use parallel execution (eg threads).","title":"Implementation notes"}]
}
